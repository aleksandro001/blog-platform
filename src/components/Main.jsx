/* eslint-disable react/prop-types */
import React from 'react';
import { Container } from '@mui/material';

const Main = ({ children }) => <Container sx={{ mt: '100px' }}>{children}</Container>;

export default Main;
